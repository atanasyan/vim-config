VIM Configuration
=================

Bundles, color schemes, syntax and config.
Checkout to the ~/.vim and create a link ~/.vimrc -> ~/.vim/vimrc.

Bundles
-------
* ctrlp
* nerdtree
* tagbar

Syntax
------
* llvm.vim
* rest.vim
* tablegen.vim
* tmux.vim
